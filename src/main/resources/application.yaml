
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/seletivo?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: seletivo
    password: seletivo123
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  logging:
    level:
      org.flywaydb: DEBUG

  jpa:
    hibernate:
      ddl-auto: validate

app:
  security:
    cors:
      allowed-origins:
        - "http://localhost:3000"
    jwt:
      secret: "SELETIVOSENIORSEPLAGMT2026VOUTENTARPASSAR"
      access-token-ttl: 5m
      refresh-token-ttl: 30m
      issuer: "seletivo-api"
    rate-limit:
      requests-per-minute: 10
